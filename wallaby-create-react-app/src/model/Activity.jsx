export class Activity {

  /* private userId: number;
  private sessions : Array<Object>; */

  constructor(userId, sessions) {
    this.userId = userId;
    this.sessions = sessions;
  }

  /*     getUserId(): number {
          return this.userId;
        }
  
        getSessions(): Array<Object> {
          return this.sessions;
        } */
}